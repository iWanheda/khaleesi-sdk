#pragma once

#include "..\..\utilities\utilities.h"
/*
struct notifications_t {
	notifications_t(color c, color c1, float time, std::string buf)
		: color(c), background_color(c1), m_time(time), m_buf(buf) { };
	color ccolor;
	color background_color;
	float m_time;
	std::string m_buf;
};

class c_notifications : public singleton < c_notifications > {
private:
	float m_text_duration = 5.f;

	std::deque<notifications_t> m_notifications;
public:
	void add(bool, color, const std::string, ...);
	void clear();
	void draw();
};*/